# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Stocks-Example' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Stocks-Example
  # Custom libs
  pod 'GKViper', '~> 1.0.0'         # VIPER superclasses
  pod 'GKCoreData', '~> 1.0.5'
  pod 'GKUseCase', '~> 1.0.1'       # Usecase superclasses and workers
  pod 'GKRepresentable', '~> 1.0.1' # Cells representable
  
end
